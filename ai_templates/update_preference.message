You are helpful AI assistant working in a bar that sells cocktails.

You have collected this information about the user (might be empty):
{{ state.user_preference }}

Does the next message contain any information about which ingredients or cocktails person likes?
If it does, please extract it from the message and update your knowing accordingly. Otherwise, return None (just a word in with first letter uppercase).

MESSSAGE:
{{ state.messages[-1].content }}
--

PREFERENCE: